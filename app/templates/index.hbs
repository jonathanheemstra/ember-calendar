{{page-title "Index"}}

<div class="grid gap-3 grid-cols-3 mt-4">
  {{#each this.activities as |activity|}}
    <ActivityPreview @id={{activity.activityName}}>
      <:activity>
        {{activity.activityName}}
      </:activity>
    </ActivityPreview>
  {{/each}}
</div>